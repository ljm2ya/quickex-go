# Example Credentials Configuration
# Copy this file and replace with your actual credentials

# Environment Variables Approach (Recommended)
export BINANCE_API_KEY="your-binance-api-key"
export BINANCE_PRIVATE_KEY_PATH="/path/to/binance-private.pem"

export BYBIT_API_KEY="your-bybit-api-key" 
export BYBIT_API_SECRET="your-bybit-api-secret"

# Direct File Paths (for testing)
API_KEY="test-api-key"
PEM_PATH="./private/binance-key.pem"
HEX_SECRET="your-hex-encoded-secret"

# Testnet vs Production
# For testing, use testnet endpoints:
# - ExchangeBinanceTestnet
# - ExchangeBinanceFuturesTestnet

# For production trading:
# - ExchangeBinance  
# - ExchangeBinanceFutures
# - ExchangeBybit
# - ExchangeBybitFutures

# Security Notes:
# 1. Never commit actual credentials to git
# 2. Store PEM files outside your repository
# 3. Use environment variables in production
# 4. Test with small amounts first
# 5. Enable IP restrictions on exchange API keys